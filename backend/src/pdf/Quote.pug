doctype html
html
  head
    link(rel="shortcut icon" type="image/png" href="/images/icons/favicon.png")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(href="https://fonts.googleapis.com/css2?family=Prodin:wght@400;500;600;700&display=swap" rel="stylesheet")
    style.
      .sheet {
        margin: 0;
        width: 100%; 
        height: 297mm;
      }

      body {
        width: 100%; 
        margin: 0;
        padding: 0px;
        background: rgb(255, 255, 255);
        font-family: 'Prodin', sans-serif;
        font-size: 11px;
        color: #222;
      }

      *, *:before, *:after {
        box-sizing: inherit;
        color: #222;
      }

      .clearfix {
        display: block;
      }

      .left {
        float: left;
      }

      .right {
        float: right;
      }

      .top-area {    
        display: block;
        width: 100%;
      }

      .alignRight {
        text-align: right !important;
      }

      .alignLeft {
        text-align: left;
      }

      .logo {
        width: 200px;
        transform: scale(1.2);
      }

      .company-info {
        text-align: right;
        font-size: 12px;
      }

      h1.invoice {
        font-size: 24px;
        color: #1a365d;
        margin-top: 0;
        font-family: 'Prodin', sans-serif;
        font-weight: 700;
      }

      p.strong {
        font-weight: 700;
      }

      .billTo p {
        text-align: right !important;
        float: right;
        display: block;
        font-size: 12px;
        width: 100%;
        margin-top: 0;
        margin-bottom: 10px;
      }

      .tableHeader {
        color: #1a365d;
        font-family: 'Prodin', sans-serif;
        font-weight: 600;
      }

      .col {
        float: left;
      }

      .col-1 { width: 4.16667%; }
      .col-2 { width: 8.33333%; }
      .col-3 { width: 12.5%; }
      .col-4 { width: 16.66667%; }
      .col-5 { width: 20.83333%; }
      .col-6 { width: 25%; }
      .col-7 { width: 29.16667%; }
      .col-8 { width: 33.33333%; }
      .col-9 { width: 37.5%; }
      .col-10 { width: 41.66667%; }
      .col-11 { width: 45.83333%; }
      .col-12 { width: 50%; }
      .col-13 { width: 54.16667%; }
      .col-14 { width: 58.33333%; }
      .col-15 { width: 62.5%; }
      .col-16 { width: 66.66667%; }
      .col-17 { width: 70.83333%; }
      .col-18 { width: 75%; }
      .col-19 { width: 79.16667%; }
      .col-20 { width: 83.33333%; }
      .col-21 { width: 87.5%; }
      .col-22 { width: 91.66667%; }
      .col-23 { width: 95.83333%; }
      .col-24 { width: 100%; }

      .space {
        margin-top: 10px;
        margin-bottom: 10px;
        width: 100%;
      }

      .table-invoice {
        width: 100%;
        border-collapse: collapse;
        border-spacing: 0;
        margin-bottom: 20px;
      }

      .table-invoice tr:nth-child(2n-1) td {
        background: #fcfeff;
      }

      .table-invoice th, .table-invoice td {
        text-align: center;
        font-family: 'Prodin', sans-serif;
      }

      .table-invoice th {
        padding: 8px 10px;
        border-bottom: 1px solid #c2e0f2;
        color: #1a365d;
        white-space: nowrap;
        font-weight: 700;
        font-size: 12px;
        text-align: right;
      }

      tbody {
        border-bottom: 1px solid #c2e0f2;
      }

      .table-invoice .service, .table-invoice .desc {
        text-align: left;
      }

      .table-invoice td {
        padding: 10px;
        text-align: right;
        font-size: 12px;
      }

      .table-invoice td.service, .table-invoice td.desc {
        vertical-align: top;
      }

      .table-invoice td.grand {
        border-top: 1px solid #5d6975;
      }

      .service span {
        display: block;
        clear: both;
        width: 100%;
        color: #666;
        margin-top: 5px;
        font-size: 10px;
      }

      .footer-invoice {
        color: #1a365d;
        text-align: center;
        padding: 15px 0;
        position: absolute;
        bottom: 20px;
        left: 0;
        right: 0;
        width: 100%;
        background: #fff;
        border-top: 2px solid #e2e8f0;
        font-size: 6px;
        line-height: 1.2;
        page-break-inside: avoid;
        z-index: 1000;
      }
      .footer-line {
        font-weight: bold;
        color: #1a365d;
        font-size: 6px;
        margin-bottom: 4px;
        text-align: center;
        line-height: 1.2;
      }

      .table-invoice td[colspan] {
        white-space: nowrap;
      }

      .table-invoice td.total {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        font-size: 11px;
        font-weight: normal;
        max-width: 150px;
      }

      .table-invoice td.grand.total {
        font-weight: bold;
        font-size: 11px;
        max-width: 150px;
      }

      .table-invoice td.service strong {
        font-weight: bold;
      }

  body
    header.clearfix
      .top-area
        img.logo.left(src=settings.public_server_file+settings.company_logo)
        .right.company-info
          p.strong #{settings.company_name}
          p #{settings.company_address}
          p #{settings.company_reg_number}
          p #{settings.company_email}
          p #{settings.company_website}
          
      .col.col-24
        .space  
      .left.col-14
        h1.invoice
          | #{translate('Devis')}
        div 
          .col.col-6
            p.strong #{translate('Date')} :
            p #{dateFormat(model.date)}
          .col.col-12
            p.strong #{'Date d\'expiration'} :
            p #{dateFormat(model.expiredDate)}
          .col.col-6
            p.strong #{'Numéro'} :
            p # #{model.number}/#{model.year || ""} 
          
      .right.col-10
        .billTo
          p.strong #{translate('Client')} :
          p #{model.client.name}
          p #{model.client.address}
          p #{model.client.phone}
          p #{model.client.email}
      .col.col-24
        .space  
    main
      table.table-invoice
        thead
          tr.tableHeader
            th.service #{'Prestations'}
            th #{'Date de début'}
            th #{'Date de fin'}
            th #{'Tarif journalier'}
            th #{translate('Total')}
        tbody
          each service in model.services
            tr
              td.service
                strong #{service.name}
                br
                span Nombre d'agents: #{service.numberOfAgents}
              td #{service.startDate ? new Date(service.startDate).toLocaleDateString('fr-FR') : ''}
              td #{service.endDate ? new Date(service.endDate).toLocaleDateString('fr-FR') : ''}
              td #{service.dailyRate.toLocaleString('fr-FR', { maximumFractionDigits: 0 })}
              td #{service.total.toLocaleString('fr-FR', { maximumFractionDigits: 0 })}

          tr
            td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
              span(style="text-align: left;") Sous-total :
              span(style="text-align: right;") #{model.subTotal.toLocaleString('fr-FR', { maximumFractionDigits: 0 })} XAF
          if model.taxDetails
            if model.taxDetails.tva
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") TVA 18%:
                  span(style="text-align: right;") #{Math.round(model.taxDetails.tva).toLocaleString('fr-FR')} XAF
            if model.taxDetails.css
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") CSS 1%:
                  span(style="text-align: right;") #{Math.round(model.taxDetails.css).toLocaleString('fr-FR')} XAF
            if model.taxDetails.tps
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") TPS -9.5%:
                  span(style="text-align: right;") -#{Math.round(model.taxDetails.tps).toLocaleString('fr-FR')} XAF
            if model.taxDetails.tax
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") #{model.taxName || 'Taxe'} (#{model.taxRate}%):
                  span(style="text-align: right;") #{Math.round(model.taxDetails.tax).toLocaleString('fr-FR')} XAF
            tr
              td(colspan="5" style="padding: 10px; font-weight: bold; display: flex; justify-content: space-between;")
                span(style="text-align: left;") Total TTC:
                span(style="text-align: right;") #{Math.round(model.total).toLocaleString('fr-FR')} XAF
          else if model.taxRate > 0
            if model.taxName === 'TVA et CSS' || model.taxRate === 19
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") TVA 18%:
                  span(style="text-align: right;") #{Math.round(model.subTotal * 0.18).toLocaleString('fr-FR')} XAF
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") CSS 1%:
                  span(style="text-align: right;") #{Math.round(model.subTotal * 0.01).toLocaleString('fr-FR')} XAF
              tr
                td(colspan="5" style="padding: 10px; font-weight: bold; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") Total TTC:
                  span(style="text-align: right;") #{Math.round(model.total).toLocaleString('fr-FR')} XAF
            else if model.taxName === 'TPS' || model.taxRate === 9.5
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") TPS -9.5%:
                  span(style="text-align: right;") -#{Math.round(model.taxDetails ? model.taxDetails.tps : model.taxTotal).toLocaleString('fr-FR')} XAF
              tr
                td(colspan="5" style="padding: 10px; font-weight: bold; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") Total TTC:
                  span(style="text-align: right;") #{Math.round(model.total).toLocaleString('fr-FR')} XAF
            else if model.taxName === 'TPS et CSS' || model.taxRate === 10.5
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") TPS -9.5%:
                  span(style="text-align: right;") -#{Math.round(model.taxDetails ? model.taxDetails.tps : model.subTotal * 0.095).toLocaleString('fr-FR')} XAF
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") CSS 1%:
                  span(style="text-align: right;") #{Math.round(model.taxDetails ? model.taxDetails.css : model.subTotal * 0.01).toLocaleString('fr-FR')} XAF
              tr
                td(colspan="5" style="padding: 10px; font-weight: bold; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") Total TTC:
                  span(style="text-align: right;") #{Math.round(model.total).toLocaleString('fr-FR')} XAF
            else if model.taxName === 'TPS CSS' || model.taxRate === 19.5
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") TPS 18.5%:
                  span(style="text-align: right;") #{Math.round(model.subTotal * 0.185).toLocaleString('fr-FR')} XAF
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") CSS 1%:
                  span(style="text-align: right;") #{Math.round(model.subTotal * 0.01).toLocaleString('fr-FR')} XAF
              tr
                td(colspan="5" style="padding: 10px; font-weight: bold; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") Total TTC:
                  span(style="text-align: right;") #{Math.round(model.total).toLocaleString('fr-FR')} XAF
            else
              tr
                td(colspan="5" style="padding: 10px; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") #{model.taxName || 'Taxe'} (#{model.taxRate}%):
                  span(style="text-align: right;") #{Math.round(model.taxTotal).toLocaleString('fr-FR')} XAF
              tr
                td(colspan="5" style="padding: 10px; font-weight: bold; display: flex; justify-content: space-between;")
                  span(style="text-align: left;") Total TTC:
                  span(style="text-align: right;") #{Math.round(model.total).toLocaleString('fr-FR')} XAF
      
    .col.col-24
      .space     
    footer.footer-invoice
      //- Pied de page avec informations DACLIN GUARDIAN SECURITY
      .footer-invoice
        div.footer-line DACLIN GUARDIAN SECURITY. SARL AU CAPITAL DE 2 000 000 FCFA. SIEGE SOCIAL CENTRE-VILLE (FEU ROUGE CENTRE MEDICO) 2641 LIBREVILLE
        div.footer-line NUMERO RCCM : GA-LBV 01 2021 B1 00297 – NIF XXXXXXXXX
        div.footer-line CONTACTS : 077131056 / 011760203 / 074808781 – DIRECTION@DGS-GABON.COM – WWW.DGS-GABON.COM

   
